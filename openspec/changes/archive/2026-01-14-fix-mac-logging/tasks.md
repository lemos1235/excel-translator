## 1. Fix Logging Layout
- [x] 1.1 Add height constraint to `logScrollView` in `cmd/mac-app/main.go`.
- [x] 1.2 Verify `SetActive(true)` is called on the constraint.

## 2. Verify Build
- [x] 2.1 Run `make mac-app` to ensure compilation succeeds.